data:
  path: data/raw/telco_customer.csv

model:
  test_size: 0.2
  random_state: 42
  
  random_forest:
    n_estimators: 100
    max_depth: 10
    

output:
  model_path: artifacts/models/model.pkl